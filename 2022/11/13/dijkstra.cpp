#include<iostream>
#include"fheap.h"

using namespace std;

const int inf = std::numeric_limits<int>::max();

int main(int argc, char **argv) {

  int s = static_cast<int>(strtol(argv[1],nullptr,10));
  int t = static_cast<int>(strtol(argv[1],nullptr,10));
  
  return 0;
  
}
